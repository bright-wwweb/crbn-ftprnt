import './Header.scss';
import React, { FC } from 'react';

const Header: FC<object> = () => {
  return (
    <header className="App-header">
      <pre> // </pre>
    </header>
  );
};

export default Header;